ext."signing.keyId" = System.getenv("SIGNING_KEYID") ?: ext."signing.keyId"
ext."signing.password" = System.getenv("SIGNING_PASSWORD") ?: ext."signing.password"
ext."signing.secretKeyRingFile" = System.getenv("SIGNING_SECRETKEYRINGFILE") ?: ext."signing.secretKeyRingFile"
